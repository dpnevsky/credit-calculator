spring:
  application:
    name: application-service

server:
  port: 8081

management:
  endpoints:
    web:
      exposure:
        include: health,info

management.endpoint.health:
  probes:
    enabled: true

integration:
  scoring:
    base-url: http://localhost:8082