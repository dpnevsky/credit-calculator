spring:
  application:
    name: scoring-service

server:
  port: 8082

management:
  endpoints:
    web:
      exposure:
        include: health,info

management.endpoint.health:
  probes:
    enabled: true